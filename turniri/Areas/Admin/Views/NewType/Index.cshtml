@model IEnumerable<turniri.Model.NewType>
@{
    ViewBag.Title = "Типы новостей";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
<h2>
    Типы новостей
</h2>
@Html.ActionLink("Создать", "Create", "NewType", null, new { @class = "btn" })
<table class="table">
    <thead>
        <tr>
            <th>
                #
            </th>
            <th>
                Наименование
            </th>
            <th>
            </th>
        </tr>
    </thead>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @item.ID
            </td>
            <td>
                @item.Name
            </td>
            <td>
                @Html.ActionLink("Изменить", "Edit", "NewType", new { id = item.ID }, new { @class = "btn" })
                @Html.ActionLink("Удалить", "Delete", "NewType", new { id = item.ID }, new { @class = "btn btn-danger" })
            </td>
        </tr>
    }
</table>
